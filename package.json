{
  "name": "dgbs-delights-local-server",
  "version": "1.0.0",
  "description": "Local static server + API to persist products.json for DGBS Delights Foods (development only)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "body-parser": "^1.20.2"
  }
}
